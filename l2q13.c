// Wap to find the value of a=5 & b=7 using bitwise AND ,bitwise OR and bitwise  NOT (USING BITWISE OPERATOR)

#include<stdio.h>
int main()
{
	int a=5,b=7;
	int AND,OR;
	AND=a&b;
	printf("AND=%d",AND);
	OR=a|b;
	printf("\nOR=%d",OR);
	return 0;
	
}
